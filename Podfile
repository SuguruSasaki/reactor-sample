# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ReactorSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReactorSample
  pod 'ReactorKit'
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'RxViewController'
  
  target 'ReactorSampleTests' do
    inherit! :search_paths    
    pod 'Quick'
    pod 'Nimble'
  end

  target 'ReactorSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
